apiVersion: apps/v1
kind: Deployment
metadata:
  name: iris-voice-gateway
  namespace: iris-ai-production
  labels:
    app.kubernetes.io/name: iris-voice-gateway
    app.kubernetes.io/part-of: iris-mx-platform
spec:
  replicas: 3
  selector:
    matchLabels:
      app: iris-voice-gateway
  template:
    metadata:
      labels:
        app: iris-voice-gateway
    spec:
      containers:
        - name: voice-gateway
          image: registry.irisxai.in/iris/voice-gateway:v2.4.0
          ports:
            - containerPort: 8088
              name: websocket
            - containerPort: 50051
              name: grpc
          resources:
            limits:
              cpu: "2000m"
              memory: "4Gi"
              nvidia.com/gpu: "1"
            requests:
              cpu: "500m"
              memory: "1Gi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8088
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8088
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: iris-voice-gateway-hpa
  namespace: iris-ai-production
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: iris-voice-gateway
  minReplicas: 3
  maxReplicas: 20
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 75
